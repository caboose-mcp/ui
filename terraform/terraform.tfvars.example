# Copy to terraform.tfvars (gitignored) and fill in values.

aws_region  = "us-east-1"
environment = "personal"
ui_domain   = "ui.mcp.chrismarasco.io"

# Set if DNS is in Route53, else add the CNAME records from acm_validation_records output manually.
route53_zone_id = ""
