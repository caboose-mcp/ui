# Copy to .env.local for local dev against a remote backend.
# In CI/CD this is set as a GitHub Actions variable, not a secret.

# URL of the caboose-mcp backend. Leave empty to use the Vite dev proxy.
VITE_API_BASE=https://mcp.chrismarasco.io
